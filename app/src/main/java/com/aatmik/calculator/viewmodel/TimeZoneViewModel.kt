// Simple TimeZoneViewModel.kt - No complex state management needed
package com.aatmik.calculator.viewmodel

import androidx.lifecycle.ViewModel

// SIMPLIFIED: No complex ViewModel needed since we're doing direct calculations
class TimeZoneViewModel : ViewModel() {
    // This ViewModel is now just a placeholder
    // All calculations are done directly in the Fragment for simplicity
}